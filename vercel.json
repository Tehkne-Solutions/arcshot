{
  "installCommand": "npm install --workspaces --include-workspace-root --include=dev --no-audit --no-fund",
  "buildCommand": "cd ../.. && npm run build -w @arcshot/game-core && npm run assets:build && npm run build -w @arcshot/game",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
